export default interface User {
  role?: any;
  address: any;
    id: number,
    firstName: string,
    lastName: string,
    phone: string,
    imageUrl: string,
    email: string,
    createdAt: string,
}